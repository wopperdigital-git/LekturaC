VITE_SUPABASE_URL=https://lgpsgimrdrfntgxjepbe.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_7e5wp12Sg3UU0MgDlNXdiA_BapN5Ut0

# Gemini key — currently unused (CreatePage.tsx is on Groq for now, see
# below), left here so switching back is a one-line change. Free at
# aistudio.google.com/apikey. Gets bundled into the client JS at build time,
# so only use this for local/personal dev — not a deployment other people
# can reach.
VITE_GEMINI_API_KEY=

# Active AI generation provider for now (Gemini was returning 503s under
# high demand). Free at console.groq.com/keys. Same bundling caveat as
# above — local/personal dev only.
VITE_GROQ_API_KEY=
